<mjml>
  <mj-body background-color="#f4f4f2">
    <mj-include path="../partials/header.mjml" />
    <mj-section background-color="#ffffff" padding="16px 24px 8px">
      <mj-column>
        <mj-text font-size="15px" line-height="1.6">
          <p>Beste {{ ontvangerNaam }},</p>
          <p>Er {% if aantalNieuw == 1 %}is <strong>1 nieuwe annotatie of reactie</strong>{% else %}zijn
          <strong>{{ aantalNieuw }} nieuwe annotaties of reacties</strong>{% endif %} geplaatst
          bij je jottem <strong>"{{ jottemTitel }}"</strong> — iemand heeft een herinnering,
          aanvulling of herkenning gedeeld.</p>
        </mj-text>
        <mj-button href="{{ jottemUrl }}" background-color="{{ organisatie.kleurPrimair }}" font-size="15px">Bekijk de reacties</mj-button>
      </mj-column>
    </mj-section>
    <mj-include path="../partials/footer.mjml" />
  </mj-body>
</mjml>
